@compiler >= 6

contract NFTReceiver = 
    stateful entrypoint on_nft_received(token_id: int, data: option(string)) : bool =
        switch(data)
            None => true
            Some(_) => false